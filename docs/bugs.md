### Bugs
[] `Lane` speed does not affect timeline as expected. Smaller numbers are faster, bigger are slower.

### TODOs
[] Catch MIDI in `motif_recorder`
[] Transform logic should live on `Motif` and `Lane` just calls it